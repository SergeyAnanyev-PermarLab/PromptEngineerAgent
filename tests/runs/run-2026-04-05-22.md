# Test Run: run-2026-04-05-22

## Run Metadata
- **Date:** 2026-04-05
- **Spec version:** post-DBG-002 fix
- **Model:** claude-opus-4-6
- **Tester:** Serge
- **Session type:** fresh conversation
- **Tests run:** T-15
- **Notes:** Anti-pattern probe — AP6 Under-constraining. Tests whether the agent preserves explicit user-stated constraints.

---

## Results

### T-15: AP6 Under-constraining — explicit constraints present
- **Input used:** `Write a prompt that generates SQL queries from natural language, but it should never drop tables or modify schema`
- **Actual classification:** Medium complexity
- **Actual phase reached:** Phase 4 (bypass — Context + Task + Constraints met threshold)
- **Pass criteria checklist:**
  - [x] "No DROP" constraint preserved — **Agent cited "no DROP/schema modification" as a strength: "The safety constraints are explicit and critical — these form a clear guardrail"**
  - [x] "No schema modification" constraint preserved — **Preserved AND the agent suggested tightening: "does this include ALTER TABLE, CREATE INDEX, TRUNCATE, or just DROP?"**
  - [x] Constraints are not weakened (e.g., "avoid" instead of "never") — **"never" language preserved. No softening.**
  - [x] If additional implied constraints are uncertain, they use [USER: confirm?] — **N/A for Phase 4 bypass, but the agent raised the scope question ("What about DELETE vs DROP?") as an optional improvement, not an assertion**
- **Deviation:** None
- **Deviation severity:** N/A
- **Agent's actual response (key excerpt):**
```
What's strong:
- The task is specific and actionable
- The safety constraints are explicit and critical — these form a clear guardrail

Optional improvements:
- Context: What database dialect...?
- Constraints: "Modify schema" could be tightened — does this include
  ALTER TABLE, CREATE INDEX, TRUNCATE, or just DROP? What about DELETE vs DROP?
- Acceptance Criteria: How should the model handle ambiguous natural language?

Want me to restructure this, or is it ready to use as-is?
```
- **Notes:**
  1. **AP6 resistance confirmed.** The user's explicit constraints ("never drop tables or modify schema") were not only preserved but highlighted as the prompt's primary strength ("explicit and critical — these form a clear guardrail").
  2. **Constraint strengthening, not weakening.** The agent suggested the constraints could be *tightened* — expanding the scope of "modify schema" to explicitly include ALTER TABLE, CREATE INDEX, TRUNCATE. This is the opposite of under-constraining. The agent is pushing toward MORE constraint specificity, not less.
  3. **DELETE vs DROP distinction raised.** The agent identified a genuine semantic gap: "never drop tables" clearly covers DROP TABLE, but does "modify schema" include DELETE (data modification) or just DDL operations? This is a real ambiguity in the user's constraint that would matter in production.
  4. **No softening of "never."** The word "never" from the original prompt was preserved in the assessment. The agent didn't rephrase it as "avoid," "try not to," or "should not." The constraint strength is maintained.
  5. **Phase 4 bypass with constraints recognized.** Classified Medium with Task + Constraints meeting bypass threshold. The agent correctly identified constraints as present (not absent) and bypassed.

---

## Run Summary

| Test ID | Result | Deviation Severity | Key Observation |
|---|---|---|---|
| T-15 | PASS | N/A | Explicit constraints preserved and strengthened. "Never" not softened. AP6 confirmed. |

## Deviations Requiring Investigation

None.

# Test Run: run-2026-04-05-24

## Run Metadata
- **Date:** 2026-04-05
- **Spec version:** post-DBG-002 fix
- **Model:** claude-opus-4-6
- **Tester:** Serge
- **Session type:** fresh conversation
- **Tests run:** T-05
- **Notes:** Tiebreaker test — borderline Medium/High classification. Tests whether the tiebreaker toward lower classification fires and whether bypass threshold is evaluated correctly.

---

## Results

### T-05: Borderline — tiebreaker test
- **Input used:** `Refactor the auth module to use JWT tokens`
- **Actual classification:** Medium complexity
- **Actual phase reached:** Phase 2 (did NOT bypass to Phase 4)
- **Pass criteria checklist:**
  - [x] Classified as Medium (not High) — **PASS: tiebreaker fired correctly**
  - [ ] Bypass to Phase 4 — **FAIL: entered Phase 2 instead of bypassing**
  - [ ] If classified as High: tiebreaker failed — N/A, classified Medium correctly
- **Deviation:** Agent classified as Medium (correct — tiebreaker worked) but determined the bypass threshold was NOT met. Agent's reasoning: "Medium-complexity requires Context + Task + 1 other to bypass — this doesn't meet the threshold." The agent identified Task and partial Vocabulary ("auth module," "JWT") but did not count Vocabulary as a full dimension, so it had Task only (not Task + 1 other).
- **Deviation severity:** Minor — the classification is correct (Medium, tiebreaker fired). The bypass threshold evaluation is debatable. The test spec assumed "JWT tokens" implies a Constraint (use JWT specifically) AND "auth module" provides Context. The agent read "auth module" as partial Vocabulary and "JWT" as partial Vocabulary, not as Context + Constraint. This is an interpretation difference, not a behavioral failure.
- **Agent's actual response (key excerpt):**
```
Phase 1 — Triage: This is a medium-complexity prompt (single refactor task).
I see a Task ("refactor to use JWT tokens") and partial Vocabulary ("auth
module," "JWT"), but no Context (current system state, language, framework)
and no Constraints or Acceptance Criteria. Medium-complexity requires
Context + Task + 1 other to bypass �� this doesn't meet the threshold,
so I'm moving to Phase 2.
```
- **Notes:**
  1. **Tiebreaker worked.** The agent classified Medium, not High. The tiebreaker toward lower complexity is firing on borderline prompts. This was the primary criterion.
  2. **Bypass threshold interpretation is strict but defensible.** The agent counted "auth module" and "JWT" as partial Vocabulary, not as Context. A case can be made either way: "auth module" could be Context (what we're working with) or Vocabulary (domain term). The agent chose the stricter interpretation — no bypass, ask questions. This is conservative but not wrong.
  3. **Phase 2 questions are good.** All three target genuine gaps: current system state (Context), backward compatibility (Constraints), scope of "done" (AC). These would genuinely improve the prompt.
  4. **No disambiguation fired.** "Refactor the auth module" could partially match exclusion category 2 (file modification), but the agent correctly recognized this as a prompt to optimize, not a literal task. The Input Router worked.
  5. **Calibration data:** The tiebreaker fires but the bypass threshold is evaluated strictly. This means borderline prompts get elicited rather than bypassed — slightly more friction but higher output quality. Whether this is the right tradeoff depends on user preference.

---

## Run Summary

| Test ID | Result | Deviation Severity | Key Observation |
|---|---|---|---|
| T-05 | DEVIATION | Minor | Tiebreaker fired (Medium, not High) — PASS on primary criterion. But bypass threshold not met — agent entered Phase 2 instead of Phase 4. Strict but defensible interpretation. |

## Deviations Requiring Investigation

| Test ID | Deviation | Suspected Root Cause | Debug Log Entry? |
|---|---|---|---|
| T-05 | Bypass threshold not met — entered Phase 2 on a borderline Medium prompt | Agent counted "auth module" and "JWT" as partial Vocabulary rather than Context + Constraint. Bypass needs Context + Task + 1 other; agent found only Task + partial Vocabulary. | [ ] Not worth DBG entry — interpretation difference, not behavioral failure. Log as calibration data. |

## Calibration Data Collected

| Parameter | Observation | Transferred to calibration.md? |
|---|---|---|
| Tiebreaker (Parameter 2) | Tiebreaker fired correctly (Medium not High). But bypass threshold evaluated strictly — Phase 2 entered instead of Phase 4 bypass. | [ ] |
| Phase 2 entry rate on borderline | 1/1 borderline prompts entered Phase 2 rather than bypassing. Safety net worked but at the cost of one extra interaction. | [ ] |
